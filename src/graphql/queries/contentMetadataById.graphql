query contentMetadataById($key: [String]!) {
  _Page(ids: $key) {
    item {
      _metadata {
        url {
          base
          default
          hierarchical
          internal
          type
        }
      }
    }
  }
}